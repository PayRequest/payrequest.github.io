---
timezone: UTC
collections:
  posts:
    title: Posts
    output: true
  uploads:
    title: Uploads
    output: true
name: PayRequest
markdown: kramdown

# Blog
paginate: 5
paginate_path: "/:num/"
date_format: "%b %-d, %Y"

plugins:
- jekyll-sitemap
- jekyll-seo-tag
- jekyll-feed
- jekyll-paginate